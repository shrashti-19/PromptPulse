const generateMockSummary = (content) => {
  return `
Summary of "${content.title}":

This note discusses key ideas related to the topic.
It contains structured information and important insights.
The content appears focused and informative.
(Mock AI response for development.)
`;
};

module.exports = { generateMockSummary };
